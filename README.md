# one-dark-theme-vscode

To use it locally:

- Open VS Code.
- Run Developer: `Install Extension from Location`
- Pick the folder `one-dark-theme-vscode`
- Reload when prompted.
- Run Preferences: Color Theme and choose `One Dark`.
